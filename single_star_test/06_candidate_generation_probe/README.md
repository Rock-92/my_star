# 06 Candidate Generation Probe

## 目的

系统评估 DAOFIND、LoG、adaptive LoG 和候选预算策略的候选 recall 上限。

## 结果

`log:3.2` 候选 oracle recall 约 `0.78~0.80`，oracle F1 高，但单图候选数约 `11~12 万`。

## 结果文件

`results/candidate_probes/` 和 `candidate_probe_smoke_20260613/`。

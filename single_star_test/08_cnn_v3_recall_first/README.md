# 08 CNN V3 Recall First

## 目的

最后尝试通过正类加权、降低 quality/offset loss、使用 class score 选模来提升 recall。

## 结果

6 张开发集最好约 `F1=0.507`，recall 仅从约 `0.365` 到 `0.371`，提升很小。

## 结果文件

`results/candidate_scorer_log3p2_recall_first_seed42*`。

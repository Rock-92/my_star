# CNN V1??? Patch Scorer ??

## ??

???? DAOFIND ????????????????? `31x31` patch????? CNN ?????????????????? DAOFIND ? recall???? CNN ??????

## ??

??? simple CNN ?? `../scripts/train_candidate_scorer.py`?????????? `../candidate_scorer/model.py` ? `CandidateScorer`?

???

1. Conv(1->16) + BN + ReLU + MaxPool
2. Conv(16->32) + BN + ReLU + MaxPool
3. Conv(32->64) + BN + ReLU + AdaptiveAvgPool
4. MLP ?? 1 ? binary logit

## ????

- ?? DAOFIND `sigma=5`??? spotcheck F1 ? `0.435`?
- DAOFIND `sigma=2.5` + simple CNN ?????? spotcheck F1 ? `0.509`?
- 3 ?? patch??? spotcheck F1 ? `0.509`???????
- hard negative mining??? spotcheck F1 ? `0.507`??????
- crop spotcheck ??? `0.56`?????? crop ????? domain gap??????????

## ????????

`../scripts/train_missed_simple_cnn.py` ??? simple CNN ????????????????? 1:1 ??????? simple CNN???????????????????

## ????

- `results/candidate_scorer_sigma2p5*`?simple CNN?3ch?resume?hard negative ??????
- `results/simple_cnn_missed_sigma2p5_seed42/`????? missed-positive simple CNN ???????
